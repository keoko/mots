#+title: Ideas

* Issues
** global leaderboard
** do not allow more than 10 scores per topic
** list of topics limited
** replace worldwide text by class members
** enter does not work to move to the next work, only tapping.
** on flashcard the pointer is lost
** add rules to avoid exploiting sqlite DB
** investigate cheaper ways to have free storage persistance
** check we do not push tokens/resources that could exploit fly account
** update new version message, 2 times
** header aligned to the left
** DONE keyboard with splitted words does not work
** DONE topics page and score pages display an scrollbar on desktop UI
* Ideas
** in bump version add tag
** use IndexedDB, any benefits?
** E2E tests, playwright issues on arch linux
** touch events API
** claude setup-install does not work on bash?
* Explore
** what is AppCache
